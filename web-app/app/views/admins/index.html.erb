<%= datagrid_form_for @grid, :method => :get, :url => admins_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<%= link_to 'back to admin path',dashboard_admin_path %>